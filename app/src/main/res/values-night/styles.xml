<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Основные -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Theme.MaterialComponents.DayNight">
        <item name="android:background">@color/dark_dark_grey</item>
    </style>

    <style name="AppTheme.ToolbarPopupOverlay" parent="ThemeOverlay.MaterialComponents.Dark" />

</resources>